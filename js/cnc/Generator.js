var pmc = pmc || {};
var cnc = cnc || {};
pmc.cnc = cnc || {};

pmc.cnc.Generator = class Generator {
    constructor(tree) {
        this.tree = tree;
    }
    
    publish(filePath){
        //let writeStream = fs.createWriteStream(filePath);
    }
}